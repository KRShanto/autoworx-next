import React from "react";

// TODO: @bettercallsundim skeleton loading screen
// BUG: This loading screen isn't showing while the data is being fetched. @bettercallsundim try to fix this.
// bettercallsundim: Couldn't find why this problem is occuring. :(
export default function Loading() {
  return <div className="text-red-500">Loading ...</div>;
}
